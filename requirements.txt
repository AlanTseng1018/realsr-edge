--extra-index-url https://download.pytorch.org/whl/cu124
torch
torchvision
numpy
opencv-python
matplotlib
scikit-image
ipykernel

# ONNX export + runtime + tooling (Stage 4 of the deploy pipeline)
onnx
onnxruntime-gpu
onnxconverter-common

# TensorRT EP for ONNX Runtime (real INT8 GPU latency benchmark).
# Ships nvinfer DLLs that ORT's TensorrtExecutionProvider depends on;
# without this, ORT silently falls back to CUDA EP at session creation.
tensorrt-cu12

# torch.compile on Windows needs Triton, which has no official Windows wheels.
triton-windows<3.3 ; sys_platform == "win32"

# Deployment / quantization runtime
onnx
onnxruntime-gpu

# torch.compile on Windows needs Triton, which has no official Windows wheels.
# triton-windows is a community-maintained build pinned for compat with PyTorch 2.6.
triton-windows<3.3 ; sys_platform == "win32"
